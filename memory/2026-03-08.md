# Daily Memory — 2026-03-08 (Sunday)

## Facebook Ads API — Token Generated ✅

- **New app created**: `GyvenimoKlubas Analytics`, App ID: `1246042837033133`
- **Access token generated** via Graph API Explorer with `ads_read` permission
- Token confirmed working: API returned `Gyvenimoklubas.lt` account, status active, EUR, spend_cap €5000
- **Ad Account ID**: `act_826084951532049`
- Token stored in dashboard Settings tab via localStorage (NOT in repo files)
- Token is a User Access Token — expires ~60 days; Aurimas must regenerate via Graph API Explorer when it expires

## Dashboard — Facebook API Integration

- Updated `dashboard/index.html`:
  - Settings form: added IDs to all FB API inputs (`fb-token`, `fb-app-id`, `fb-ad-account`)
  - `connectFacebook()`: saves to localStorage, calls API
  - `loadFbData()`: fetches account info + campaign insights (last 30 days)
  - `updateDashboardWithInsights()`: rebuilds KPI cards and charts with live data
  - Auto-loads on page load if token in localStorage
  - "Refresh" button appears after connecting
- KPI cards need IDs added (`kpi-spend`, `kpi-impr`, `kpi-clicks`, `kpi-leads`, `kpi-purchases`) — was in progress at compaction
- Dashboard deployed at: `https://aurimaslevickas.github.io/aras-dashboard/`

## Session State at Compaction

- Was in the middle of updating dashboard KPI card IDs so JavaScript can update them dynamically
- Next: finish KPI IDs → push to GitHub → verify in browser → then test with real token
- Chrome Browser Relay: still `cdpReady: false` — not yet resolved

## Key Credentials (locations only — no values in memory)

- FB Access Token: stored in dashboard localStorage only (not in any file)
- FB App ID: `1246042837033133`
- Ad Account ID: `act_826084951532049`
- GitHub PAT location: summary context (not in workspace files)

## Pending Tasks

1. Finish KPI card ID additions to dashboard HTML
2. Push dashboard update to `aras-dashboard` GitHub repo
3. Verify live FB data loads in browser
4. Add Stripe integration to dashboard when Aurimas provides key
5. visitvilnius.lt: still running locally; tunnel is ephemeral
6. Antras Pavasaris menopause funnel: not started yet
